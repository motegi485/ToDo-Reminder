node_modules/
dist/
dist-ssr/
.env
.env.local
.env.*.local
_codexReview/*

.vite/
.wrangler/
.DS_Store

*.log

# TypeScript incremental build info
*.tsbuildinfo

# Vite generated types cache
.vite-types/

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json

# Cloudflare Wrangler local secrets / config (use wrangler.toml.example as template)
.dev.vars
.dev.vars.*
wrangler.toml

# D1 manual export (contains real user data)
backup-*.sql

# 設計ドキュメント（いったん追跡外。追跡に切り替えるときはこの行を削除する）
docs/
